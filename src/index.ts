import { StateProvider } from "./components";
import { types as AkStateTypes } from "./helpers";
import { reduxInit } from "./helpers";

export const AkState = {
  reduxInit,
  StateProvider,
};

export type { AkStateTypes };
